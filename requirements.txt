{
  "name": "python-api-rest-fastapi",
  "version": "1.0.0",
  "description": "REST API con Python FastAPI",
  "main": "main.py",
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "dev": "uvicorn main:app --reload",
    "test": "pytest -v"
  },
  "dependencies": {
    "fastapi": "^0.104.1",
    "uvicorn": "^0.24.0",
    "pydantic": "^2.5.0",
    "python-dotenv": "^1.0.0"
  },
  "devDependencies": {
    "pytest": "^7.4.0",
    "httpx": "^0.25.0"
  }
}
